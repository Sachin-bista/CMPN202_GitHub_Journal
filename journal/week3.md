# Week 3 – Package Installation

Focused on installing necessary benchmarking tools and resolving package issues.

## Tasks:
- Installed `stress-ng`, `fio`, `iperf3`, `nginx`, and `sysstat`

```bash
sudo apt install stress-ng fio iperf3 nginx sysstat -y
```
![Package Install](../images/week3_package_install.jpeg)

